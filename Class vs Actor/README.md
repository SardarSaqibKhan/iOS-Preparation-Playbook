## Class vs Actor

Both are value type and store on heap. the key difference is classes are not thread safe by default whereas Actors are design in way that prevent race condtion and its thread safe.

#### Class

